# Warehouse cable (planned package)

Future home for Cube soft-fail adapter:

| Current | Role |
|---------|------|
| `../cube_module.py` | center → heart → standalone |

Hermespace must never become a second MemoryProvider. Cube owns durable memory;
Space consumes arterial strips and seals decisions back.
