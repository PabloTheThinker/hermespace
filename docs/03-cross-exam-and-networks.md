# Cross-exam and networks

Hermespace combines:

- Anthropic J-space *roles* (limited, reportable workspace)  
- Classical WM / GWT / load theory  
- Multi-stream encode/decode (Meta Brain&AI *roles*)  
- Agent memory patterns (episodic ring, semantic consolidate, prompt inject)  

It is a **harness global workspace**, callable via CLI and Hermes plugin — not a paper-only process.
