# Meta Brain & AI → Hermespace reverse map

Public Meta FAIR lines (Brain2Qwerty decode; TRIBE-class multi-stream encode):

| Meta idea | Hermespace |
|-----------|------------|
| Encode multimodal stimulus | `streams.encode_stimulus` |
| Decode internal state → language | `streams.decode_to_report` → Report |
| Production hierarchy | intention → semantics → report |

**Not claimed:** reading anyone’s brain; matching Meta accuracy; scanner hardware.

Code: `src/hermespace/streams.py`
