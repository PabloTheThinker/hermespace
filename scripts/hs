#!/usr/bin/env bash
# Convenience wrapper — no install required
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
export PYTHONPATH="${ROOT}/src${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m hermespace.cli "$@"
